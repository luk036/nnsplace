from math import floor

import networkx as nx

from .max_parametric import max_parametric


def set_default(G: nx.DiGraph, weight, value):
    """[summary]

    Arguments:
        G (nx.DiGraph): directed graph
        weight ([type]): [description]
        value ([type]): [description]
    """
    for u, v in G.edges:
        if G[u][v].get(weight, None) is None:
            G[u][v][weight] = value


def min_cycle_ratio(G: nx.DiGraph, dist, update_ok, r0, care_io=False):
    """[summary]

    Arguments:
        G ([type]): [description]

    Returns:
        [type]: [description]
    """
    # TODO: change to max_cycle_ratio

    # mu = 'cost'
    # sigma = 'time'
    # set_default(G, mu, 1)
    # set_default(G, sigma, 1)
    # T = type(dist[next(iter(G))])

    def calc_weight(r, e):
        """[summary]

        Arguments:
            r ([type]): [description]
            e ([type]): [description]

        Returns:
            [type]: [description]
        """
        u, v = e
        return floor(G[u][v]['cost'] - r * G[u][v]['time'])

    def calc_ratio(C):
        """Calculate the ratio of the cycle

        Arguments:
            C {list}: cycle list

        Returns:
            cycle ratio
        """
        total_cost = sum(G[u][v]['cost'] for (u, v) in C)
        total_time = sum(G[u][v]['time'] for (u, v) in C)
        return floor(total_cost / total_time)

    # C0 = nx.find_cycle(G)
    # r0 = calc_ratio(C0)
    return max_parametric(G, r0, calc_weight, calc_ratio, dist, update_ok,
                          io=care_io)
